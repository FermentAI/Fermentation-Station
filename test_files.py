import unittest
import rms
from rms import main2

class Testfiles(unittest.TestCase):

    def test_path(display_page):
        isExist = os.path.exists(pathname)
        assertTrue(isExist)
    def test_file(display_page):
        isFile = os.path.isfile(pathname)
        assertTrue(isFile)
